*.png
!icon.png
dev/ropdebee_eca.user.js
# test run artifacts (screenshots + the measurements beside them) — the PNGs
# were already ignored, the JSON next to them was not and got swept into a
# commit by a directory-wide `git add`
test/shots/
